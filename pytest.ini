[pytest]
markers =
    e2e: marks tests that execute end-to-end benchmarking workflows
    integration: marks tests that require actual model execution (deselect with '-m "not integration"')
    slow: marks tests as slow (deselect with '-m "not slow"')
